# Security policy

OpenApps does not execute installation commands or send catalog data over the network
by default. Treat exported deployment instructions as review material, not as a
security guarantee.

Report vulnerabilities privately through GitHub's security advisory flow. Include
reproduction steps, affected version or commit, impact, and a suggested mitigation
when available. Do not publish an exploitable proof of concept before a fix is ready.
